
public class Main {

    public static void main(String[] args){
        Event.arriveToCodecool();
        if (Event.balint.canplay) {Event.randomDestroy();}
        if (Event.balint.canplay) {Event.preparation();}
        if (Event.balint.canplay) {Event.randomDestroy();}
        if (Event.balint.canplay) {Event.meetingRoom();}
        if (Event.balint.canplay) {Event.randomDestroy();}
        if (Event.balint.canplay) {Event.lunchBreak();}
        if (Event.balint.canplay) {Event.randomDestroy();}
        if (Event.balint.canplay) {Event.afternon();}
    }
}
